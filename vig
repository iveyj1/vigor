#!/bin/bash
set -euo pipefail

root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec python3 -c '
import runpy, sys
sys.path.insert(0, sys.argv.pop(1))
runpy.run_module("vigor", run_name="__main__")
' "$root" "$@"
